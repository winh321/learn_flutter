add stateless widget to get hot reload

  stless - keyword

  class Second extends StatelessWidget {
  const Second({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}

usage 

  void main(){
  return runApp(MaterialApp(
      home:Home()
  ));
}
